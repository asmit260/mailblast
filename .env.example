# BulkMailer — Environment Variables (optional)
# Copy to .env if you want to pre-fill defaults

PORT=3000

# Optional: pre-fill SMTP defaults (user can override in UI)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_SECURE=false
